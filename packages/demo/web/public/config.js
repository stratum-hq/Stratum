/* global window */
// Runtime demo config. Overwritten at container start by the web entrypoint with
// the API key the demo seed mints. Empty here so the committed tree holds no key.
window.__DEMO_API_KEY__ = "";
